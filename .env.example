# Directory where workspace/ lives. Defaults to <repo root>/workspace when unset.
# WIKI_WORKSPACE_DIR=

# Which WorldRepository/AssetStore implementation to use. Only "filesystem" exists today.
# PERSISTENCE_DRIVER=filesystem
